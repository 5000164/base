type Task {
  id: Int!
  name: String!
}

type Query {
  tasks: [Task!]!
}

type Mutation {
  addTask(name: String!): Task!
  updateTask(id: Int!, name: String!): Task!
}

schema {
  query: Query
  mutation: Mutation
}
